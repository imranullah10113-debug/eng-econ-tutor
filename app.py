import streamlit as st
# from openai import OpenAI # Uncomment if using API, limiting for test first to ensure app loads

st.title("Engineering Economy Tutor")
st.write("If you see this text, the app is working!")
st.write("Now we just need to add the logic.")

# We start simple to verify deployment works.
